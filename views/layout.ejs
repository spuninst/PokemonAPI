<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body class="bg-teal-900 min-h-screen">
    <nav class="bg-teal-900 text-white shadow-md">
      <div class="container mx-auto px-4 py-3">
        <div class="flex justify-between items-center">
          <h1 class="text-xl font-bold">PokéTracker</h1>
          <div class="space-x-4">
            <a href="/" class="hover:underline">Dashboard</a>
            <% if (locals.user) { %>
            <a href="/favorites" class="hover:underline">Favorites</a>
            <a href="/timeline" class="hover:underline">Timeline</a>
            <div class="inline-block relative group">
              <button class="hover:underline"><%= user.username %> ▼</button>
              <div
                class="absolute right-0 hidden group-hover:block bg-white text-gray-800 shadow-lg rounded-md w-32 mt-1"
              >
                <a href="/logout" class="block px-4 py-2 hover:bg-gray-100"
                  >Logout</a
                >
              </div>
            </div>
            <% } else { %>
            <a href="/login" class="hover:underline">Login</a>
            <a href="/signup" class="hover:underline">Sign Up</a>
            <% } %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto p-4"><%- body %></main>

    <footer class="bg-gray-200 py-4 mt-8">
      <div class="container mx-auto px-4 text-center text-gray-600">
        <p>PokéTracker &copy; 2025</p>
      </div>
    </footer>
  </body>
</html>
